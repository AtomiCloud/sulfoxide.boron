serviceTree:
  landscape: suicune
  cluster: opal-ruby
  platform: systems
  service: internal-ingress
  module: tunnel
  layer: "1"

replicaCount: 1

autoscaling:
  enabled: false

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 150m
    memory: 128Mi

auth:
  secretName: cloudflare-tunnel-token
  internal:
    enable: false
  external:
    enable: true
    refreshInterval: 1h
    remoteSecretName: /suicune/auto/cloudflared/opal-ruby-cloudflared-ingress-token
    secretStore:
      name: aws-ssm-secret-store
      kind: ClusterSecretStore
